fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
email-validator==2.2.0
supabase==2.10.0
pyjwt==2.10.1
cryptography==44.0.0
python-multipart==0.0.20

# Phase 5 — ML performance predictor (offline training + serving)
scikit-learn==1.6.1
pandas==2.2.3
numpy==2.2.1
joblib==1.4.2

# Testing (dev)
pytest==8.3.4
# Required by fastapi.testclient, and also a supabase transitive dep — which is
# what constrains the version: supabase 2.10.0 requires httpx>=0.26,<0.28, so
# anything 0.28+ makes the install unresolvable. 0.27.2 is the version actually
# verified against this codebase.
httpx==0.27.2
